# 연구 방향성 및 가설 정리

## 1. 현재 연구 방향

현재 연구의 핵심은 단순히 "한국어 OCR 모델의 인식률을 비교"하는 것이 아니라, **정형 한국어 문서에서 OCR 텍스트 좌표와 표 구조 좌표가 어디에서 불안정해지는지 확인하고, ROI 기반 접근이 핵심 필드 추출 정확도를 개선하는지 검증하는 것**이다.

특히 임대차계약서는 다음 특성이 있다.

- 표와 병합 셀이 많다.
- `보증금`, `계약금`, `임대인`, `임차인`, `소재지`, `계약기간` 등 핵심 필드의 위치가 비교적 정형적이다.
- 전체 표를 완벽하게 복원하지 않아도 핵심 필드 주변의 ROI를 안정적으로 특정하면 정보 추출이 가능할 수 있다.

따라서 연구 문제를 다음과 같이 구분한다.

1. **Text Detection 문제**: OCR이 한글 텍스트의 Bounding Box 자체를 안정적으로 반환하는가?
2. **Table Structure 문제**: 반환된 텍스트와 행/열/병합 셀 구조가 올바르게 대응되는가?
3. **Field Mapping 문제**: 검출된 텍스트를 `보증금`, `주소`, `임대인` 등 실제 의미 필드에 안정적으로 연결할 수 있는가?
4. **ROI Re-OCR 문제**: 핵심 영역을 다시 잘라 OCR하면 전체 이미지 OCR보다 필드 정확도가 개선되는가?

---

## 2. 현재까지의 해석 수정

기존 예비조사 결과에서 "한국어 문서에서는 좌표가 붕괴한다"고 단정하기보다는 다음과 같이 해석하는 것이 적절하다.

> 한국어 자체가 Text Detection Bounding Box 붕괴의 직접적인 원인이라고 보기보다, 복잡한 표·병합 셀·읽기 순서·셀과 텍스트의 대응 과정에서 구조적 오류가 발생할 가능성이 크다.

즉, **OCR 좌표 검출 실패와 Table Structure Recognition 실패를 분리해서 평가해야 한다.**

또한 기존 PaddleOCR 2.x / PP-Structure 실험 결과만으로 Paddle 계열 전체가 한국어 계약서에서 사용할 수 없다고 결론 내리면 안 된다. 최신 PP-StructureV3, PP-OCRv5 Korean 등은 별도 재검증이 필요하다.

TATR(Table Transformer)도 단순히 "셀 좌표 반환 불가"라고 보기보다, row/column/spanning-cell 결과를 이용해 cell bbox를 재구성할 수 있는지 확인하고, **재구성된 셀 좌표가 실제 한국어 임대차계약서 병합 셀과 일치하는지**를 평가해야 한다.

---

## 3. 연구 가설

### H1. OCR Text Bounding Box는 한국어 자체보다 이미지 품질의 영향을 더 크게 받는다.

**가설**  
한국어 정형 문서에서 OCR Text Detection Bounding Box는 비교적 안정적으로 검출되지만, 기울기·저해상도·블러·표선 겹침·촬영 왜곡 등의 이미지 품질이 좌표 안정성에 더 큰 영향을 준다.

**검증 방법**
- PaddleOCR 단독 실행
- 원본 이미지와 전처리 이미지 비교
- Text bbox IoU 또는 검출 성공률 비교

---

### H2. 주요 오류는 Text Detection보다 Table Structure 및 Cell-Text Mapping에서 증가한다.

**가설**  
텍스트 영역 자체는 검출되더라도, 행/열/병합 셀 복원과 읽기 순서 결정 과정에서 오류가 증가하여 핵심 필드 매핑이 불안정해진다.

**검증 방법**
- Text bbox와 cell bbox를 별도로 시각화
- 동일 문서에서 OCR 결과와 구조 모델 결과 비교
- 각 텍스트가 정답 셀에 포함되는지 평가

---

### H3. 병합 셀은 범용 Table Structure Recognition 모델의 주요 실패 지점이다.

**가설**  
단순 행/열 구조보다 rowspan/colspan이 포함된 임대차계약서 영역에서 cell reconstruction 오류가 크게 증가한다.

**검증 방법**
- 일반 셀 / 병합 셀을 구분한 Ground Truth 구성
- PP-StructureV3, TATR 등에서 각각 cell bbox 추출
- 병합 셀과 일반 셀의 IoU, Precision, Recall 비교

---

### H4. 표선 기반 ROI 검출은 정형 계약서에서 핵심 필드 영역을 안정적으로 특정할 수 있다.

**가설**  
표선이 비교적 명확한 정형 임대차계약서에서는 OpenCV 기반 line/cell detection이 범용 Table Structure Recognition보다 핵심 필드의 공간 영역을 안정적으로 특정할 수 있다.

**주의**  
현재는 가설이며 "OpenCV가 최적"이라고 선결론을 내리지 않는다.

**검증 방법**
- OpenCV ROI와 구조 모델 ROI 비교
- ROI bbox IoU
- 핵심 필드 ROI 검출 성공률
- 실패 사례 유형 분석

---

### H5. ROI 재인식은 전체 이미지 OCR 후 좌표 매핑보다 핵심 필드 정확도를 향상시킨다.

**가설**  
정확하게 특정된 ROI에서 OCR을 다시 수행하면 전체 문서 OCR 결과에서 좌표 기반으로 필드를 매핑하는 방식보다 핵심 정보 추출 정확도가 향상된다.

**비교 파이프라인**

```text
Baseline
전체 이미지 OCR
  -> text bbox
  -> 좌표/규칙 기반 필드 매핑
  -> 핵심 필드 추출

Proposed
셀 또는 Anchor 검출
  -> ROI Crop
  -> ROI별 OCR
  -> 필드별 후처리
  -> 핵심 필드 추출
```

**평가 지표**
- Field Exact Match
- Field Accuracy
- CER
- 처리 시간(sec/image)

---

### H6. 전체 표 복원보다 Anchor-ROI 방식이 핵심 필드 추출에 더 적합할 수 있다.

**가설**  
전체 표의 모든 셀을 정확하게 복원하는 것보다 `보증금`, `계약금`, `임대인`, `임차인` 등의 라벨 위치를 Anchor로 사용하고 상대적인 공간 영역을 탐색하는 방식이 핵심 정보 추출에서 더 단순하고 안정적일 수 있다.

예시:

```text
"보증금" 라벨 bbox 검출
        -> 오른쪽/인접 영역 ROI 설정
        -> ROI OCR
        -> 금액 형식 후처리
```

이 가설은 임대차계약서에만 종속되는 규칙을 만드는 것이 아니라, **유사한 라벨-공간 관계를 가지는 정형 문서에서의 적용 가능성**까지 검토하는 방향으로 확장한다.

---

## 4. 우선 비교할 파이프라인

초기 실험에서는 모델 수를 지나치게 늘리지 않고 다음 네 가지를 우선 비교한다.

| 구분 | 방법 | 주요 확인 대상 |
|---|---|---|
| A | PaddleOCR 단독 | Text bbox baseline |
| B | PP-StructureV3 + Korean OCR | 최신 Paddle 구조/셀 좌표 |
| C | TATR + Korean OCR | 구조 모델과 OCR 분리 시 cell reconstruction |
| D | OpenCV + Korean OCR | 물리적 선 기반 ROI |

필요 시 추가 후보로 Surya OCR/Table, Docling TableFormer 등을 검토한다.

---

## 5. 논문의 중심 질문

최종적으로는 다음 질문에 답하는 방향으로 실험을 구성한다.

> **한국어 정형 표 문서에서 Text Bounding Box와 Cell Bounding Box는 각각 어느 단계에서 불안정해지며, 공간 구조를 활용한 ROI 재인식이 핵심 필드 추출 정확도를 실제로 향상시키는가?**

현재 단계에서는 특정 모델이 최고라고 결론 내리지 않고, **좌표 검출 → 구조 복원 → 필드 매핑 → ROI 재인식 → 최종 필드 정확도**의 단계별 영향을 분리해 검증한다.

# 담당자 피드백

## 대상 작업

`dahye_cell_DetectionSurvey` 브랜치의 표 구조 인식 모델 비교 조사 및 README 정리.

---

## 1. 잘 된 점

### 1) 연구 문제와 직접 연결되는 비교 기준을 잡은 점

단순 모델 소개가 아니라 다음 기준으로 비교한 것은 연구 방향과 잘 맞는다.

- 셀 단위 좌표 반환 가능 여부
- 한국어 임대차계약서 적용 가능성
- CPU 환경 실행 가능 여부
- 설치 난이도
- 처리 시간
- OpenCV 방식 대비 장점

이 기준은 이후 본 실험에서 모델 후보를 빠르게 거르는 데 유용하다.

### 2) 실제 실행 환경과 실패 사례를 기록한 점

실행 성공 결과뿐 아니라 다음과 같은 실패도 남긴 점이 좋다.

- PP-Structure의 언어 설정 문제
- LayoutParser / Detectron2의 Windows·의존성 문제
- SLANet의 구조 붕괴
- TATR의 복잡한 병합 셀 처리 한계

논문에서는 실패 자체도 중요하지만, **성능 실패인지 환경 실패인지 구분하여 기록**해야 한다.

### 3) OpenCV + OCR이라는 후속 방향을 제시한 점

범용 구조 모델이 바로 해결하지 못한 상황에서, 정형 문서의 물리적 표선을 활용하여 ROI를 먼저 나누고 OCR을 적용하는 방향을 제안한 것은 다음 실험으로 연결하기 좋다.

---

## 2. 수정이 필요한 해석

### 1) `실행 실패`와 `모델 성능 실패`를 분리해야 함

현재 README 일부 표현은 실행 환경 문제와 실제 모델 성능 한계를 같은 수준으로 보이게 할 수 있다.

예:

- LayoutParser가 Windows/CPU 환경에서 실행되지 않은 경우
  - `셀 검출 성능이 낮다`가 아니라
  - `현재 실험 환경에서 평가하지 못했다`가 정확함.

- PP-Structure가 특정 버전에서 `lang='korean'` 문제로 실행되지 않은 경우
  - `PP-Structure는 한국어를 지원하지 않는다`로 일반화하지 않고
  - `사용한 버전/구성에서는 한국어 설정으로 평가가 제한되었다`고 쓰는 것이 안전함.

### 2) TATR의 셀 좌표 해석을 수정할 필요가 있음

기존 README의 `외곽선 및 행/열만, 세부 셀 불가`는 너무 강한 표현이다.

TATR는 row / column / spanning-cell 등의 구조 결과에서 cell 영역을 후처리로 재구성할 수 있으므로, 다음과 같이 표현하는 것이 더 적절하다.

> 본 임대차계약서 실험에서는 TATR가 생성한 구조 결과로부터 목표 병합 셀을 안정적으로 재구성하지 못하였다.

즉, 질문은

- `TATR가 셀 bbox를 반환 가능한가?`

보다

- `TATR가 재구성한 셀 bbox가 실제 계약서의 병합 셀 Ground Truth와 일치하는가?`

가 되어야 한다.

### 3) `한국어라서 좌표가 붕괴한다`는 결론을 피해야 함

OCR의 Text Detection과 한국어 Recognition은 분리해서 봐야 한다.

텍스트 bbox가 흔들리는 주요 원인 후보는 다음과 같다.

- 촬영 기울기
- 저해상도
- Blur
- 표선과 문자 겹침
- 작은 글씨
- 원근 왜곡
- 스캔 노이즈

한국어는 Recognition 및 구조 매핑에 영향을 줄 수 있지만, 좌표 붕괴의 직접 원인이라고 단정해서는 안 된다.

### 4) `완벽`, `최적`, `가장 확실` 표현은 제거 필요

현재 단계는 예비조사이므로 OpenCV 방식이 더 좋아 보인다는 관찰과 실제 우수성을 검증한 결과를 구분해야 한다.

피해야 할 표현:

- `완벽하게 오려낸다`
- `최적의 대안이다`
- `가장 확실하다`

권장 표현:

> 예비 실험 결과를 바탕으로 정형 표선 기반 ROI 검출 방식의 적용 가능성을 확인하였으며, 후속 실험에서 범용 구조 모델과 정량 비교한다.

---

## 3. 최신 버전 재검증 권장

현재 조사 결과는 당시 사용한 버전 기준으로 의미가 있으나, 다음 항목은 최신 버전으로 다시 확인할 가치가 있다.

### PP-StructureV3

확인 항목:

- 한국어 OCR 모델과 결합 가능 여부
- `cell_box_list` 등 셀 좌표 반환 여부
- 병합 셀 복원 품질
- 이전 PP-Structure 실험과의 차이

### PP-OCRv5 Korean

확인 항목:

- 전체 이미지에서 text bbox 안정성
- 한국어 Recognition 정확도
- ROI Crop 후 재인식 시 정확도 변화

### TATR

확인 항목:

- row / column / spanning-cell 결과 저장
- 후처리로 cell bbox 재구성
- 일반 셀과 병합 셀을 분리하여 성능 비교

---

## 4. 다음 README 개선 제안

다음 업데이트에서는 비교표를 아래처럼 바꾸면 논문 자료로 활용하기 좋아진다.

| 모델 | 실행 성공 | Text bbox | Cell bbox | 병합 셀 | 한국어 OCR | 제외/한계 사유 |
|---|---|---|---|---|---|---|
| PaddleOCR | O | O | - | - | O | 구조 정보 없음 |
| PP-StructureV3 | 확인 필요 | O | 확인 | 확인 | O | 후속 실험 |
| TATR | O | OCR 별도 | 재구성 가능 | 검증 필요 | 별도 OCR | 복잡 구조 평가 필요 |
| LayoutParser | X | 평가 불가 | 평가 불가 | 평가 불가 | - | 환경 문제 |
| OpenCV + OCR | O | OCR 사용 | O | 규칙 기반 | O | 표선 품질 의존 |

그리고 가능하면 결과 이미지를 동일한 계약서 한 장 기준으로 나란히 남긴다.

- 원본
- text bbox
- cell bbox
- 병합 셀 결과
- 최종 ROI
- OCR 결과

---

## 5. 담당자에게 요청하는 핵심 보완 사항

1. 기존 조사 결과를 삭제하지 말고 **버전과 실행 환경을 명확히 남길 것**.
2. `실행 실패`와 `성능 실패`를 구분할 것.
3. TATR 셀 재구성 가능 여부를 코드 기준으로 다시 확인할 것.
4. PP-StructureV3 + Korean OCR 최신 조합을 한 번 재실험할 것.
5. 결과를 성공/실패 서술로만 남기지 말고 가능하면 bbox 이미지와 정량 지표를 남길 것.
6. 최종 결론은 `OpenCV가 최고`가 아니라 `OpenCV ROI 방식이 유망하여 본 실험 비교 대상으로 선정됨` 정도로 수정할 것.

---

## 한 줄 피드백

> 현재 조사는 **연구 방향을 정하는 예비 탐색 자료로 충분히 가치가 있으며**, 다음 단계에서는 모델의 지원 여부를 단정하는 것보다 동일한 계약서에서 Text Detection, Cell Reconstruction, 병합 셀, 최종 필드 추출을 분리해 비교하는 방향으로 발전시키면 좋다.

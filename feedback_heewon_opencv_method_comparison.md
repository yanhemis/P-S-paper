# 담당자 피드백 — `heewon`

> 마지막 업데이트: 2026-09-11

## 이 문서의 역할

이 파일은 `heewon` 브랜치의 **OpenCV 기반 Cell Detection 방법 비교 실험에 대한 피드백만** 기록한다.

`bang_`의 병합 셀 재구성 알고리즘 피드백, `dahye_cell_DetectionSurvey`의 구조 모델/평가 코드 피드백, 공통 연구 방향은 각각 다른 문서에서 관리한다.

---

## 1. 현재 작업의 의미

현재 브랜치는 다음 두 OpenCV 방법을 비교하는 역할로 보는 것이 적절하다.

1. Morphological Opening 기반 수평/수직선 추출 후 grid bbox 생성
2. Contour 기반 셀 bbox 검출

추가로 결과 bbox 이미지와 Pipeline / Sequence Diagram이 있어, **OpenCV 방식 내부에서 어떤 셀 검출 전략이 더 적절한지 비교하는 실험군**으로 활용할 수 있다.

이 담당자의 역할은 `bang_`처럼 병합 셀 최종 재구성 로직을 만드는 것이 아니라, **Morphology와 Contour 방식의 특성과 파라미터 민감도를 비교하는 것**으로 분리한다.

---

## 2. 잘 된 점

### 2.1 짧은 내부 선 검출을 위한 kernel 비율 조정

기본 함수에는 `h_ratio=0.4`, `v_ratio=0.4`가 남아 있지만 실제 실행에서는 `0.05` 값을 사용하여 짧은 표선도 남길 수 있도록 실험한 점이 좋다.

이는 임대차계약서처럼 페이지 전체 폭보다 훨씬 짧은 내부 경계가 많은 문서에서 필요한 방향이다.

### 2.2 Morphology와 Contour를 동일 문서에서 비교할 수 있는 기반 생성

두 방법 모두 bbox 결과 이미지가 존재하므로, 앞으로 동일 GT에 연결하면 단순 시각 비교를 넘어 정량 비교가 가능하다.

### 2.3 파이프라인/시퀀스 다이어그램 작성

구현 흐름을 설명하는 자료가 있어 향후 논문 시스템 구성도나 발표 자료로 발전시키기 좋다.

---

## 3. 이번 버전에서 확인할 핵심 문제

### 3.1 Morphological 방식에서 모든 셀이 병합 후보로 판정됨

현재 출력에서는 다음 결과가 확인된다.

```text
이미지 크기: 3000 x 4000
검출된 행 개수: 37
검출된 열 개수: 4
검출된 셀 개수: 148
병합 셀 후보 개수: 148
```

즉, 검출된 모든 기본 셀이 merged 후보로 분류되고 있다.

이는 실제 문서의 모든 셀이 병합되어 있다는 의미라기보다 **현재 경계선 존재 여부 판단 로직 또는 threshold가 정상적으로 작동하지 않을 가능성**을 먼저 의심해야 한다.

이 담당자는 병합 셀 최종 재구성까지 맡지 않고 다음까지만 확인하면 된다.

- [ ] 왜 대부분의 right/bottom boundary가 missing으로 판정되는지 시각화
- [ ] presence ratio 변경에 따른 후보 수 변화 기록
- [ ] line mask 자체가 너무 얇거나 끊겨 있는지 확인
- [ ] 일반 셀 몇 개를 수동으로 골라 boundary 판정이 맞는지 디버깅

최종 merged bbox 생성은 `bang_` 담당 범위로 넘긴다.

---

### 3.2 Contour 방식의 263개 bbox를 성능으로 해석하면 안 됨

Contour 방식은 동일 샘플에서 263개의 bbox를 검출했다.

하지만 현재 필터는 대략 다음 조건 중심이다.

- `min_width=50`
- `min_height=20`
- 전체 페이지의 95%보다 작은 contour

따라서 실제 셀이 아닌 텍스트 덩어리, 내부 사각형, 중첩 contour 등이 포함되었을 가능성이 있다.

필수 확인:
- [ ] bbox 개수 자체가 아니라 GT와의 일치율 계산
- [ ] 너무 작은/큰 bbox 필터 threshold 민감도 비교
- [ ] 중첩 bbox 제거(NMS 또는 containment filtering) 필요 여부 확인
- [ ] RETR_LIST와 다른 retrieval mode 차이 검토

---

## 4. 이 담당자에게 권장하는 비교 실험

같은 샘플에 대해 다음 표를 채우는 것이 가장 좋다.

| 방법 | 주요 파라미터 | bbox 수 | Cell Precision | Cell Recall | F1 | 처리 시간 |
|---|---|---:|---:|---:|---:|---:|
| Morphology Grid | h/v ratio |  |  |  |  |  |
| Contour | min w/h |  |  |  |  |  |

추가로 실패 유형을 다음처럼 구분한다.

- 짧은 내부 선 누락
- 끊어진 선
- 희미한 선
- 텍스트 획 오검출
- 중복 contour
- 셀 내부 사각 요소 오검출
- 병합 셀 경계 미검출

---

## 5. 파라미터 민감도 실험 제안

모든 조합을 돌릴 필요는 없고, 대표값만 비교한다.

### Morphology

예:
- h/v ratio: `0.03`, `0.05`, `0.08`, `0.1`

확인:
- 너무 크면 짧은 내부 경계 제거
- 너무 작으면 문자 획이 선으로 살아남는지

### Contour

예:
- min_width / min_height 2~3조합
- closing kernel 2~3조합

확인:
- bbox 수 변화
- Precision / Recall 변화

최종 목적은 최적 파라미터 탐색 자체가 아니라 **어떤 방식이 계약서 구조에 더 안정적인지 설명 가능한 근거를 얻는 것**이다.

---

## 6. `bang_` 담당과의 경계

겹치지 않도록 다음처럼 역할을 분리한다.

### `heewon`
- Morphology vs Contour 비교
- parameter sensitivity
- bbox 생성 품질 비교
- 실패 사례 분류

### `bang_`
- 선/grid 결과에서 실제 merged cell bbox 재구성
- 경계선 기반 병합 로직 개선
- 최종 ROI 생성 로직

따라서 `heewon`에서 병합 셀 후보 판정 문제가 확인되면 원인과 재현 조건까지만 정리하고, 최종 재병합 알고리즘 개발은 넘긴다.

---

## 7. 1차 완료 기준

- [ ] Morphology / Contour 결과를 동일 GT evaluator에 연결
- [ ] bbox 수가 아닌 Precision / Recall / F1 기록
- [ ] Morphology ratio 최소 3개 비교
- [ ] Contour filter 최소 2~3조합 비교
- [ ] 대표 실패 사례 이미지 저장
- [ ] 모든 셀이 merged 후보가 되는 원인 분석
- [ ] 최종적으로 두 방식의 장단점을 표로 정리

---

## 한 줄 피드백

> 현재 브랜치는 OpenCV 셀 검출 방식 비교군으로 가치가 크다. 앞으로는 bbox 개수보다 **동일 GT에서 Morphology와 Contour의 Precision/Recall과 파라미터 민감도를 비교하는 역할**에 집중하면 `bang_` 담당과도 겹치지 않고 연구 기여가 명확해진다.

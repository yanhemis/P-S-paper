# 담당자 피드백 — `bang_`

## 이 문서의 역할

이 파일은 `bang_` 브랜치의 **OpenCV 기반 표선/셀/ROI 검출 프로토타입에 대한 피드백만** 기록한다.

구조 모델 조사 피드백은 `feedback_dahye_cell_detection_survey.md`에서 관리하고, 공통 연구 방향과 전체 다음 작업은 `01_research_direction_and_next_steps.md`에서 관리한다.

---

## 1. 현재 작업의 의미

이 브랜치는 연구 가설 중 다음 부분을 직접 구현한 프로토타입에 해당한다.

> 정형 임대차계약서에서 물리적 표선을 이용한 OpenCV 기반 Cell/ROI Detection이 실제로 가능한가?

현재 구현에는 다음 요소가 들어가 있다.

- CLAHE + adaptive threshold를 이용한 대비 강화/이진화
- 수평/수직 morphology 기반 표선 추출
- morphology closing을 이용한 끊어진 선 보정
- connected component 길이 기준의 문자 획 오검출 제거
- grid 좌표 추출
- 경계선 유무 기반 병합 셀 후보 탐지
- rows × cols 기반 cell bbox 생성
- 처리 시간 출력

즉, **OpenCV Cell Detection baseline prototype**으로는 의미가 있다.

---

## 2. 현재 코드에서 가장 중요한 보완점

### 2.1 병합 셀 후보 탐지와 실제 bbox 재구성이 연결되지 않음

현재 `detect_merged_cells()`는 오른쪽/아래쪽 경계가 없는 셀을 병합 후보로 기록한다.

하지만 최종 bbox는 `get_cell_boxes_from_grid(rows, cols)`에서 모든 `rows × cols` 조합으로 생성된다.

따라서 현재 상태는 다음과 같이 표현하는 것이 정확하다.

> 병합 셀 **후보 탐지는 구현**, 병합 정보를 반영한 **최종 merged bbox 재구성은 미구현**.

다음 단계에서는 병합 후보 정보를 이용하여 인접 기본 셀들을 실제 하나의 bbox로 합치는 로직이 필요하다.

---

### 2.2 morphology kernel 비율이 짧은 내부 경계선을 제거할 가능성

현재 수평/수직 선 추출 커널은 이미지 폭/높이의 약 40%를 기준으로 잡는다.

임대차계약서에는 페이지 전체 길이에 비해 짧은 내부 셀 경계가 많기 때문에, 고정된 큰 커널을 사용하면 짧은 선이 opening 단계에서 사라질 수 있다.

확인할 것:

- [ ] kernel ratio를 여러 값으로 바꿔 민감도 실험
- [ ] 긴 외곽선과 짧은 내부 경계선의 검출률을 분리
- [ ] 해상도 변화에 따라 같은 ratio가 유지되는지 확인

현재 실패가 발생하더라도 `OpenCV 자체 실패`가 아니라 **현재 파라미터와 선 추출 방식의 한계인지 분리해서 기록**해야 한다.

---

### 2.3 `close_gaps()`의 수직선 보정 길이 기준 점검

현재 closing 길이가 `img_w` 기준 하나로 계산되어 수평/수직 모두에 사용된다.

수평선과 수직선은 다음처럼 별도로 두는 것이 더 자연스럽다.

```python
h_close_len = int(img_w * ratio)
v_close_len = int(img_h * ratio)
```

세로가 긴 문서에서 폭 기준 길이를 그대로 수직선에 쓰면 과도하거나 부족한 closing이 생길 수 있다.

---

### 2.4 고정 threshold/ratio 파라미터의 일반화 검증 필요

현재 주요 고정값 예:

- `h_ratio=0.4`
- `v_ratio=0.4`
- component 최소 길이 약 0.15
- `gap_thresh=10`
- `presence_ratio=0.5`

이 값들이 샘플 한 장에 맞춰진 것인지, 다른 계약서에서도 유지되는지 확인해야 한다.

다음 실험에서는 파라미터를 바꿔도 성능이 유지되는 범위를 기록하는 것이 좋다.

---

## 3. 현재 결과를 해석할 때 주의할 점

### `병합 셀 처리 가능`은 아직 강한 표현

현재는 병합 후보를 찾는 단계이므로 다음 표현이 적절하다.

> 경계선 누락을 이용한 병합 셀 후보 탐지는 가능하며, 실제 병합 bbox 재구성은 후속 구현이 필요하다.

### `셀 개수`만으로 정확도를 판단하면 안 됨

현재 출력은 주로 다음이다.

- 행 수
- 열 수
- 셀 수
- 병합 후보 수
- 처리 시간

검출 셀 개수가 정답 개수와 같아도 위치가 틀릴 수 있으므로 Ground Truth와의 IoU 평가가 필요하다.

### 처리 시간 일반화 주의

샘플 한 장의 실행 시간을 전체 데이터셋의 성능으로 일반화하지 않는다.

최소 여러 장에서 평균/분산 또는 median을 기록하는 것이 좋다.

---

## 4. 이 담당자에게 우선 요청할 작업

- [ ] `merged_cells` 결과를 실제 merged bbox 생성에 반영
- [ ] 일반 셀 bbox와 병합 셀 bbox를 서로 다른 색으로 시각화
- [ ] 짧은 내부 경계선 누락 사례 확인
- [ ] kernel ratio 파라미터 민감도 테스트
- [ ] `close_gaps()` 수평/수직 길이 기준 분리
- [ ] 샘플 1장이 아닌 공통 Ground Truth 이미지 세트에서 실행
- [ ] Cell IoU / Precision / Recall 계산
- [ ] 실패 사례를 `흐린 선 / 끊어진 선 / 병합 셀 / 짧은 경계 / 기울기` 등으로 분류

---

## 5. 이후 연구에 직접 연결되는 실험

이 브랜치의 결과는 다음 순서로 확장하면 된다.

```text
현재 OpenCV Cell Detection
  -> merged bbox 재구성
  -> Ground Truth와 Cell IoU 평가
  -> 핵심 필드 ROI crop
  -> ROI별 Korean OCR
  -> 전체 이미지 OCR과 Field Accuracy 비교
```

만약 전체 Cell Reconstruction이 계속 불안정하면, 모든 셀을 복원하려 하지 않고 핵심 라벨을 기준으로 ROI를 잡는 Anchor-ROI 방식으로 연결할 수 있다.

---

## 6. README에 추가하면 좋은 내용

현재 브랜치 README에는 최소 다음 내용을 남기는 것이 좋다.

- 실험 목적
- 입력 샘플 특성
- 처리 흐름
- 주요 파라미터
- 검출 결과 이미지
- 검출 행/열/셀 수
- 처리 시간
- 병합 셀 처리 상태
- 대표 실패 사례
- 아직 구현되지 않은 항목

특히 `성공`과 `미구현`을 구분해서 적는 것이 중요하다.

---

## 한 줄 피드백

> 현재 구현은 OpenCV 기반 Cell/ROI Detection 가능성을 확인하는 좋은 1차 프로토타입이며, 다음 단계에서는 병합 후보 탐지를 실제 merged bbox 재구성으로 연결하고 동일 Ground Truth에서 정량 평가하는 것이 가장 중요하다.
